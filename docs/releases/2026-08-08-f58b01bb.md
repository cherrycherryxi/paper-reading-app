# 生产版本更新说明

> 发布日期：2026-08-08  
> 版本提交：`f58b01bb6f8ddbad986561b9d1f85aee699f1258`  
> 上一版本：`c58981b407202941cea622d6ca775064d640259a`  
> 本次提交数：41

## 一句话概览

本次发布围绕「feat(memory): add confirmed reading memories」完成了一组产品改进，重点是让阅读记录、内容采集和日常使用更加稳定、顺畅。

## 解决了什么问题

- 本次发布涉及的用户问题和缺陷，来自以下变更记录：
- `c5fa660` feat(memory): add confirmed reading memories
- `8043478` chore(wolf): pre-sync knowledge snapshot
- `cb8de66` fix(chat): keep history when clear request fails (#109)
- `220d56f` chore(wolf): pre-sync knowledge snapshot
- `cb1cc30` chore(explore): Codex cloud directions 2026-08-06
- `bfafb92` chore(explore): Codex cloud directions 2026-08-06
- `3d78bb6` chore(triage): Codex cloud triage 2026-08-06
- `d8fa4e6` chore(triage): Codex cloud triage 2026-08-05
- `1840382` feat(codex): migrate nightly agents to cloud schedules
- `e6fab7e` chore(wolf): record implementation completion
- `c1eac96` chore(wolf): record book list UX changes
- `ce56b70` feat: simplify book card metadata (#107)
- `9977757` feat: add searchable book tag filters (#106)
- `d8a48ec` chore(wolf): pre-sync knowledge snapshot
- `ba4e766` chore(triage): Codex nightly triage 2026-08-04
- `a384dfd` chore(codex): track agent config and OpenWolf state
- `074fbb1` test(ci): remove ripgrep dependency
- `7653538` feat: migrate nightly agents to Codex
- `9a5c740` chore(wolf): pre-sync knowledge snapshot
- `1e5edf7` fix(chat): recover quote-scoped history context (#104)
- `6f5f58b` chore(wolf): pre-sync knowledge snapshot
- `a30964d` fix(agent): skip duplicate thought links (#103)
- `b4a91e3` chore(wolf): record completion status
- `3beb8b7` chore(wolf): record daily card implementation
- `c0ce2d5` feat: show book deletion impact (#102)
- `4005420` feat: add book session overview (#101)
- `34ddc76` chore(wolf): pre-sync knowledge snapshot
- `8053343` chore(product-owner): Codex 周一焦点 2026-W32
- `556ecbc` docs(signals): 北极星周记 2026-W32 (Codex 自动)
- `9f6e698` feat: migrate weekly product loop to Codex
- `183efe3` chore(wolf): pre-PO snapshot 2026-W32
- `fc1e95f` docs(signals): 北极星周记 2026-W31 (自动)
- `d1c7397` feat: configure DeepSeek model via environment
- `6bc8478` chore: record book-list experience signals

## 新功能与改进

### 变更明细

### `c5fa660` feat(memory): add confirmed reading memories

**存在的问题**：feat(memory): add confirmed reading memories（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `c5fa660` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `8043478` chore(wolf): pre-sync knowledge snapshot

**存在的问题**：chore(wolf): pre-sync knowledge snapshot（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `8043478` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `cb8de66` fix(chat): keep history when clear request fails (#109)

**存在的问题**：fix(chat): keep history when clear request fails (#109)（发布后可补充真实用户触发场景）。

**目标**：消除该问题，恢复相关流程的稳定表现。

**实现方法**：详见提交 `cb8de66` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `220d56f` chore(wolf): pre-sync knowledge snapshot

**存在的问题**：chore(wolf): pre-sync knowledge snapshot（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `220d56f` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `cb1cc30` chore(explore): Codex cloud directions 2026-08-06

**存在的问题**：chore(explore): Codex cloud directions 2026-08-06（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `cb1cc30` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `bfafb92` chore(explore): Codex cloud directions 2026-08-06

**存在的问题**：chore(explore): Codex cloud directions 2026-08-06（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `bfafb92` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `3d78bb6` chore(triage): Codex cloud triage 2026-08-06

**存在的问题**：chore(triage): Codex cloud triage 2026-08-06（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `3d78bb6` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `d8fa4e6` chore(triage): Codex cloud triage 2026-08-05

**存在的问题**：chore(triage): Codex cloud triage 2026-08-05（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `d8fa4e6` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `1840382` feat(codex): migrate nightly agents to cloud schedules

**存在的问题**：feat(codex): migrate nightly agents to cloud schedules（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `1840382` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `e6fab7e` chore(wolf): record implementation completion

**存在的问题**：chore(wolf): record implementation completion（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `e6fab7e` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `c1eac96` chore(wolf): record book list UX changes

**存在的问题**：chore(wolf): record book list UX changes（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `c1eac96` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `ce56b70` feat: simplify book card metadata (#107)

**存在的问题**：feat: simplify book card metadata (#107)（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `ce56b70` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `9977757` feat: add searchable book tag filters (#106)

**存在的问题**：feat: add searchable book tag filters (#106)（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `9977757` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `d8a48ec` chore(wolf): pre-sync knowledge snapshot

**存在的问题**：chore(wolf): pre-sync knowledge snapshot（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `d8a48ec` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `ba4e766` chore(triage): Codex nightly triage 2026-08-04

**存在的问题**：chore(triage): Codex nightly triage 2026-08-04（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `ba4e766` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `a384dfd` chore(codex): track agent config and OpenWolf state

**存在的问题**：chore(codex): track agent config and OpenWolf state（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `a384dfd` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `074fbb1` test(ci): remove ripgrep dependency

**存在的问题**：test(ci): remove ripgrep dependency（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `074fbb1` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `7653538` feat: migrate nightly agents to Codex

**存在的问题**：feat: migrate nightly agents to Codex（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `7653538` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `9a5c740` chore(wolf): pre-sync knowledge snapshot

**存在的问题**：chore(wolf): pre-sync knowledge snapshot（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `9a5c740` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `1e5edf7` fix(chat): recover quote-scoped history context (#104)

**存在的问题**：fix(chat): recover quote-scoped history context (#104)（发布后可补充真实用户触发场景）。

**目标**：消除该问题，恢复相关流程的稳定表现。

**实现方法**：详见提交 `1e5edf7` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `6f5f58b` chore(wolf): pre-sync knowledge snapshot

**存在的问题**：chore(wolf): pre-sync knowledge snapshot（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `6f5f58b` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `a30964d` fix(agent): skip duplicate thought links (#103)

**存在的问题**：fix(agent): skip duplicate thought links (#103)（发布后可补充真实用户触发场景）。

**目标**：消除该问题，恢复相关流程的稳定表现。

**实现方法**：详见提交 `a30964d` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `b4a91e3` chore(wolf): record completion status

**存在的问题**：chore(wolf): record completion status（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `b4a91e3` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `3beb8b7` chore(wolf): record daily card implementation

**存在的问题**：chore(wolf): record daily card implementation（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `3beb8b7` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `c0ce2d5` feat: show book deletion impact (#102)

**存在的问题**：feat: show book deletion impact (#102)（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `c0ce2d5` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `4005420` feat: add book session overview (#101)

**存在的问题**：feat: add book session overview (#101)（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `4005420` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `34ddc76` chore(wolf): pre-sync knowledge snapshot

**存在的问题**：chore(wolf): pre-sync knowledge snapshot（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `34ddc76` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `8053343` chore(product-owner): Codex 周一焦点 2026-W32

**存在的问题**：chore(product-owner): Codex 周一焦点 2026-W32（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `8053343` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `556ecbc` docs(signals): 北极星周记 2026-W32 (Codex 自动)

**存在的问题**：docs(signals): 北极星周记 2026-W32 (Codex 自动)（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `556ecbc` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `9f6e698` feat: migrate weekly product loop to Codex

**存在的问题**：feat: migrate weekly product loop to Codex（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `9f6e698` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `183efe3` chore(wolf): pre-PO snapshot 2026-W32

**存在的问题**：chore(wolf): pre-PO snapshot 2026-W32（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `183efe3` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `fc1e95f` docs(signals): 北极星周记 2026-W31 (自动)

**存在的问题**：docs(signals): 北极星周记 2026-W31 (自动)（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `fc1e95f` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `d1c7397` feat: configure DeepSeek model via environment

**存在的问题**：feat: configure DeepSeek model via environment（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `d1c7397` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `6bc8478` chore: record book-list experience signals

**存在的问题**：chore: record book-list experience signals（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `6bc8478` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### 面向用户的变化

- 具体功能、修复和体验变化见上方提交明细；其中带有 `OPT-`、`bug-` 或 `fix` 标记的条目代表已纳入本次发布范围的问题闭环。
- 不改变现有账号数据结构和使用方式；已有数据会沿用现有兼容逻辑。

### 实现与质量保障

- 代码变更统计：

```text
 .agents/skills/nightly-product-agents/SKILL.md     |  69 +++++
 .../nightly-product-agents/agents/openai.yaml      |   4 +
 .codex/agents/ux-reviewer.toml                     | 252 +++++++++++++++++
 .env.example                                       |   2 +
 .wolf/anatomy.md                                   | 130 +++++++--
 .wolf/memory.md                                    | 295 ++++++++++++++++++++
 README.md                                          |   3 +
 app.js                                             | 298 ++++++++++++++++-----
 app_server.py                                      | 116 ++++++--
 chat.js                                            |   8 +-
 docker-compose.yml                                 |   1 +
 docs/codex-cloud-scheduled-migration.md            |  57 ++++
 docs/codex-project-migration.md                    |  64 ++++-
 index.html                                         |  33 ++-
 optimization/backlog.md                            | 102 +++++--
 optimization/explore.md                            |  62 +++++
 optimization/roadmap.md                            |  13 +-
 optimization/signals.md                            |   5 +
 optimization/triage.md                             |  96 +++----
 scripts/codex/README.md                            |  55 +++-
 scripts/codex/cloud-setup.sh                       |  14 +
 ...om.huangnanqi.paper-codex-nightly-explore.plist |  19 ++
 ....huangnanqi.paper-codex-nightly-implement.plist |  16 ++
 ...com.huangnanqi.paper-codex-nightly-triage.plist |  16 ++
 .../com.huangnanqi.paper-codex-product-owner.plist |  20 ++
 .../com.huangnanqi.paper-codex-weekly-report.plist |  20 ++
 scripts/codex/nightly-explore.sh                   |  99 +++++++
 scripts/codex/nightly-implement.sh                 | 139 ++++++++++
 scripts/codex/nightly-triage.sh                    | 114 ++++++++
 scripts/codex/product-owner-monday.sh              | 124 +++++++++
 scripts/codex/weekly-report.sh                     | 140 ++++++++++
 styles.css                                         | 159 +++++++++--
 tests/agent/action_executor_link_thought_test.py   | 103 +++++++
 tests/agent/chat_history_context_test.py           |  46 ++++
 tests/agent/codex_cloud_scheduled_test.py          |  72 +++++
 tests/agent/codex_nightly_automation_test.py       | 179 +++++++++++++
 tests/agent/codex_weekly_automation_test.py        | 112 ++++++++
 tests/agent/custom_quote_tags_sanitize_test.py     |   8 +
 tests/agent/deepseek_retry_test.py                 |  20 ++
 tests/agent/deployment_config_test.py              |   4 +-
 tests/agent/prompt_builder_memories_test.py        |  23 ++
 tests/frontend/account-import-format.test.js       |  30 +++
 tests/frontend/book-card-hierarchy.test.js         | 106 ++++++++
 tests/frontend/book-detail-ux.test.js              |  34 +++
 tests/frontend/book-tag-filter-more.test.js        | 115 ++++++++
 tests/frontend/dialog-escape-cleanup.test.js       |  51 ++++
 tests/frontend/regression-fixed-bugs.test.js       |  38 +++
 47 files changed, 3257 insertions(+), 229 deletions(-)
```

- 变更文件：

```text
A	.agents/skills/nightly-product-agents/SKILL.md
A	.agents/skills/nightly-product-agents/agents/openai.yaml
A	.codex/agents/ux-reviewer.toml
M	.env.example
M	.wolf/anatomy.md
M	.wolf/memory.md
M	README.md
M	app.js
M	app_server.py
M	chat.js
M	docker-compose.yml
A	docs/codex-cloud-scheduled-migration.md
M	docs/codex-project-migration.md
M	index.html
M	optimization/backlog.md
M	optimization/explore.md
M	optimization/roadmap.md
M	optimization/signals.md
M	optimization/triage.md
M	scripts/codex/README.md
A	scripts/codex/cloud-setup.sh
A	scripts/codex/launchd/com.huangnanqi.paper-codex-nightly-explore.plist
A	scripts/codex/launchd/com.huangnanqi.paper-codex-nightly-implement.plist
A	scripts/codex/launchd/com.huangnanqi.paper-codex-nightly-triage.plist
A	scripts/codex/launchd/com.huangnanqi.paper-codex-product-owner.plist
A	scripts/codex/launchd/com.huangnanqi.paper-codex-weekly-report.plist
A	scripts/codex/nightly-explore.sh
A	scripts/codex/nightly-implement.sh
A	scripts/codex/nightly-triage.sh
A	scripts/codex/product-owner-monday.sh
A	scripts/codex/weekly-report.sh
M	styles.css
A	tests/agent/action_executor_link_thought_test.py
A	tests/agent/chat_history_context_test.py
A	tests/agent/codex_cloud_scheduled_test.py
A	tests/agent/codex_nightly_automation_test.py
A	tests/agent/codex_weekly_automation_test.py
M	tests/agent/custom_quote_tags_sanitize_test.py
M	tests/agent/deepseek_retry_test.py
M	tests/agent/deployment_config_test.py
A	tests/agent/prompt_builder_memories_test.py
M	tests/frontend/account-import-format.test.js
A	tests/frontend/book-card-hierarchy.test.js
M	tests/frontend/book-detail-ux.test.js
A	tests/frontend/book-tag-filter-more.test.js
M	tests/frontend/dialog-escape-cleanup.test.js
M	tests/frontend/regression-fixed-bugs.test.js
```

## 最终效果与验证

- 发布目标：生产环境 `main` 指针。
- 服务动作：Prod 工作树快进更新后重启现有服务。
- 发布前必须通过项目的前端和 Python 回归测试；发布后必须检查本地 Prod 接口与公网入口。

## 影响范围

- 前端：以变更文件列表为准。
- 后端/API：以变更文件列表为准。
- 数据迁移：本次发布脚本不会主动修改生产数据库；如提交包含迁移，需在这里补充迁移说明。

## 回滚信息

如需回滚，将生产代码快进/切换回上一版本 `c58981b407202941cea622d6ca775064d640259a`，然后重启 Prod 服务；生产数据库和用户上传文件不会被发布脚本删除。

## 发布记录

- 生成方式：`scripts/codex/release-hook.sh`
- 本文档由生产发布 hook 自动生成；发布后可人工补充「面向用户的变化」中的业务描述。
