# 生产版本更新说明

> 发布日期：2026-07-31  
> 版本提交：`f077a09e7bc74e589673f1dac3869bdecbe0bc93`  
> 上一版本：`99d193fa067cb10c2176f9a8dce2ce8da0290c2a`  
> 本次提交数：2

## 一句话概览

本次发布围绕「chore: record release hook session」完成了一组产品改进，重点是让阅读记录、内容采集和日常使用更加稳定、顺畅。

## 解决了什么问题

- 本次发布涉及的用户问题和缺陷，来自以下变更记录：
- `f077a09` chore: record release hook session
- `deea3da` feat: automate production release notes

## 新功能与改进

### 变更明细

### `f077a09` chore: record release hook session

**存在的问题**：chore: record release hook session（发布后可补充真实用户触发场景）。

**目标**：完成本次发布范围内的工程或产品改进。

**实现方法**：详见提交 `f077a09` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### `deea3da` feat: automate production release notes

**存在的问题**：feat: automate production release notes（发布后可补充真实用户触发场景）。

**目标**：补充或优化对应产品能力，降低使用成本。

**实现方法**：详见提交 `deea3da` 及下方变更文件列表。

**最终效果**：已纳入本次生产发布，发布后的接口和公网入口由发布脚本自动校验。

### 面向用户的变化

- 具体功能、修复和体验变化见上方提交明细；其中带有 `OPT-`、`bug-` 或 `fix` 标记的条目代表已纳入本次发布范围的问题闭环。
- 不改变现有账号数据结构和使用方式；已有数据会沿用现有兼容逻辑。

### 实现与质量保障

- 代码变更统计：

```text
 .wolf/anatomy.md              |  12 +++--
 .wolf/memory.md               |   8 +++
 AGENTS.md                     |   4 ++
 docs/releases/README.md       |  17 ++++++
 scripts/codex/deploy-prod.sh  |  53 ++++++++++++++++++
 scripts/codex/release-hook.sh | 121 ++++++++++++++++++++++++++++++++++++++++++
 6 files changed, 212 insertions(+), 3 deletions(-)
```

- 变更文件：

```text
M	.wolf/anatomy.md
M	.wolf/memory.md
M	AGENTS.md
A	docs/releases/README.md
A	scripts/codex/deploy-prod.sh
A	scripts/codex/release-hook.sh
```

## 最终效果与验证

- 发布目标：生产环境 `main` 指针。
- 服务动作：Prod 工作树快进更新后重启现有服务。
- 发布前必须通过项目的前端和 Python 回归测试；发布后必须检查本地 Prod 接口与公网入口。

## 影响范围

- 前端：以变更文件列表为准。
- 后端/API：以变更文件列表为准。
- 数据迁移：本次发布脚本不会主动修改生产数据库；如提交包含迁移，需在这里补充迁移说明。

## 回滚信息

如需回滚，将生产代码快进/切换回上一版本 `99d193fa067cb10c2176f9a8dce2ce8da0290c2a`，然后重启 Prod 服务；生产数据库和用户上传文件不会被发布脚本删除。

## 发布记录

- 生成方式：`scripts/codex/release-hook.sh`
- 本文档由生产发布 hook 自动生成；发布后可人工补充「面向用户的变化」中的业务描述。
